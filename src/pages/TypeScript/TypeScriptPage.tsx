import React from "react";
import { Typography } from "@mui/material";

const TypeScriptPage: React.FunctionComponent = () => {
  return (
    <React.Fragment>
      <Typography variant="h4">
        In this section I will make links to my repository on the git.
        distributed on advanced topics of working with TS. I will ignore the
        simple things like basic types and simple tools, etc.
      </Typography>
      <pre><code></code></pre>
    </React.Fragment>
  );
};

export default TypeScriptPage;
